<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

<!-- 按壓時 -->
<item android:state_pressed="true">
    <!--rectangle 長方形 可以畫出直角形，圓角形，弧形等-->
    <shape
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="oval">
        <padding
            android:bottom="4dp"
            android:left="4dp"
            android:right="4dp"
            android:top="4dp" /> //設置內間距

        <size
            android:width="50dp"
            android:height="50dp" /> //設置圓形大小

        <color android:color="#E3FFFF" />
        <stroke
            android:width="2px"
            android:color="#000000" />

    </shape>
</item>

<!-- 預設時 -->
<item android:color="@android:color/black">
    <shape
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="oval">
        <padding
            android:bottom="4dp"
            android:left="4dp"
            android:right="4dp"
            android:top="4dp" /> //設置內間距

        <size
            android:width="50dp"
            android:height="50dp" /> //設置圓形大小

        <color android:color="@color/transparent" />
        <stroke
            android:width="2px"
            android:color="#000000" />

    </shape>
</item>

</selector>